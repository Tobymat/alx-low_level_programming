#include <stdio.h>
#include "main.h"

/**
 * _islower - entry point
 * Return: 0 if false and 1 if otherwise
 */

int _islower(int c)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
	{
		return (1);
	}
	return (0);
