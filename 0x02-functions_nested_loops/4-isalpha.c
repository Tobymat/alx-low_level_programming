#include <stdio.h>
#include "main.h"

/**
 * _isaplha - checks if character is an alphabet
 * @c: character to check
 */

int _isalpha(int c)
{
	return ((c = 'a' && c = 'z' || (c = 'A' && c = 'Z'));
}
