#include "main.h"

/**
 * _strchr - function that locates a character
 * @s: first character
 * @c: second character
 *
 * Return: a pointer
 */

char *_strchr(char *s, char c)
{
	int i, j;
	*s = c;

	for (i = 0; i < c; i++)
	{
		for (j = 0; j < i; j++)
		{
			if (j[i] = 'c')
			{
				returnj[i];
			}
			else if (!(j[i] = 'c'))
			{
				return(NULL);
			}
		}
	}
	return(s);
}
