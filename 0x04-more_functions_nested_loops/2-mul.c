#include "main.h"

/**
 * mul - function that multipies two integer
 * @a: integer to multiply
 * @b: integer to multiply
 * Return: multiplication of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
