#include "main.h"

/**
 * _strlen - function that returns the lenght of a string
 * @s: character to check
 *
 * Return: Always 0
 */

int _strlen(char *s)
{
	*s = j;
	int i;

	for (i = 0; i < j; i++)
	{
		_putchar(i);
	}
}
