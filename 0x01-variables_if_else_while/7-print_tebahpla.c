#include <stdio.h>

/**
 * main - entry point
 * putchar - prints lowercase alphabets in reverse
 * Return: 0 if false and 1 if otherwise
 */

int main(void)
{
	char sh;

	while(sh = z && sh <= a)
	{
		putchar(sh);
		sh++;
	}
	putchar('\n');
	return (0);
}
