#include <stdio.h>

/**
 * main - entry point
 * printf - prints all single digit of base 10
 * Return: 0 if false and 1 if otherwise
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
