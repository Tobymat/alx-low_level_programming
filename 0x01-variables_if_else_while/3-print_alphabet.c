#include <stdio.h>

/**
 * main - entry point
 * putchar - prints 'lowercase and uppercase alphabets'
 * Return: 0 if false and 1 if true
 */

int main(void)
{
	char ah;

	for (ah = 'a'; ah < 'z'; ah++)
	{
		putchar(ah);
	} else if (ah = 'A'; ah < 'Z'; ah++)
	{
		putchar(ah);
	}
	putchar('\n');
	return (0);
}
