#include "main.h"

/**
 * print_number - entry point
 * @n: integer counter
 * Description: a function that prints an integer
 * Return: Always (0)
 */

void print_number(int n)
{
	for (n = 'a'; n <= 'z'; n++)
	{
		if (!(n = 'a' && <= 'z'))
		{
			_putchar(n);
		}
	}
	_putchar('\n');
}
